# Stride Athletes – Run Club

Node/Express App, statische Seite + Endpunkte (`/api/register`, `/api/confirm`, `/api/clubs`).

## Development
```bash
npm install
cp .env.example .env   # Werte setzen
npm start              # http://localhost:3000
